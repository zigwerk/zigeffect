# zigeffect-http

Real bounded HTTP server and client lifecycle adapters for `zigeffect-std`.

The package starts at `local_development` maturity. It can earn
`production_candidate` only through the live socket, recovery, redaction,
shutdown, and bounded-load conformance lanes defined by the production platform
roadmap.

```sh
zig build test
```
