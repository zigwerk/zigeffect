pub const tagged = @import("tagged.zig");

pub const exhaustive = tagged.exhaustive;
pub const partial = tagged.partial;
pub const orElse = tagged.orElse;
pub const option = tagged.option;
pub const either = tagged.either;
