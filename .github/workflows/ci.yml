name: ci

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  ZIG_VERSION: 0.16.0

jobs:
  packages:
    name: package boundary
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: mlugg/setup-zig@v2.2.1
        with:
          version: ${{ env.ZIG_VERSION }}
      - name: Verify deterministic consumer archives
        run: scripts/test_package_release.sh
      - name: Verify source installer
        run: scripts/test_install.sh
      - name: Verify Zig-native TLS adapter
        working-directory: packages/zigeffect-http-tls-zigtls
        run: zig build test
      - name: Verify tool and Testing v2 hygiene
        run: |
          packages/zigeffect/tools/check_tool_hygiene.sh
          packages/zigeffect/scripts/check_testing_v2_migration.sh

  bootstrap:
    name: CLI bootstrap
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: mlugg/setup-zig@v2.2.1
        with:
          version: ${{ env.ZIG_VERSION }}
      - name: Verify project metadata and CLI
        run: |
          (cd packages/zigeffect-std && zig build test)
          (cd packages/zigeffect-cli && zig build test)
          (cd packages/zigeffect-cli && zig build integration-test)

  graph-packages:
    name: graph packages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: mlugg/setup-zig@v2.2.1
        with:
          version: ${{ env.ZIG_VERSION }}
      - name: Verify graph package boundary
        run: |
          (cd packages/zgdb && zig build test)
          (cd packages/zgroach && zig build test)
          (cd packages/zgraphy && zig build test)

  core:
    name: native core
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: mlugg/setup-zig@v2.2.1
        with:
          version: ${{ env.ZIG_VERSION }}
      - name: Run Testing v2 core suite
        working-directory: packages/zigeffect
        run: zig build test-raw --summary all

  frontend:
    name: frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run check:frontend
